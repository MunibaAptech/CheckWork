@model IEnumerable<tbl_Category>
@{
    ViewBag.Title = "ShowList";
}
<script>
    function Confirmation() {

         confirm("Are you sure you want to delete");
    }
</script>
<h2>ShowList</h2>

<table class= "table table-bordered table-striped" >
    <thaed class= " btn-primary">
        <tr>
            <th>
                Category ID
            </th>
            <th>
                Category Name
            </th>
            <th>
                Category Description
            </th>
            <th></th>
        </tr>
    </thaed>
    <tbody>
     @foreach (var item in Model)
     {
        <tr>
            <td>@item.CategoryId</td>
            <td>@item.Name</td>
            <td>@item.Description</td>

            <td>@Html.ActionLink("Edit","Edit","Home",new { id = item.CategoryId},htmlAttributes:new { @class = "btn btn-primary", @onclick = "Confirmation()" }) 
                @Html.ActionLink("Delete", "Delete", "Home", new { cateegory_id = item.CategoryId }, htmlAttributes: new { @class = "btn btn-danger" })
            </td>

           



            @*<td>@Html.ActionLink("Edit", "Edit", "Home", new { id = item.CategoryId }, htmlAttributes: new { @class = "btn btn-primary" })
            @Html.ActionLink("Delete", "Delete", "Home", new { category_id = item.CategoryId }, htmlAttributes: new { @class = "btn btn-danger",onclick = "return Confirmation()"}) 
            </td>*@
            
        </tr>
     }
        
    </tbody>
    

</table>

